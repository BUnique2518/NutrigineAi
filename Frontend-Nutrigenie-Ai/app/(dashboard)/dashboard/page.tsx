import DashboardClient from "@/components/dashboard/DashboardClient";

// Its only job is to render the main client component.

export default function DashboardPage() {
  return <DashboardClient />;
}